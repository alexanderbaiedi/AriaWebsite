{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "preinstall": "node ./scripts/enforce-pnpm.mjs",
    "build": "corepack pnpm run typecheck && corepack pnpm --filter @workspace/aria-website --filter @workspace/api-server --if-present run build",
    "start": "corepack pnpm --filter @workspace/api-server start",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
